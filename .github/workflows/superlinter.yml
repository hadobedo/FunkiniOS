name: iOS build

on:
  [push, workflow_dispatch]
jobs:
  build:
    runs-on: macOS-latest
    steps:
      - uses: krdlab/setup-haxe@v1
        with:
          haxe-version: 4.0.5
      - run: |
          haxelib setup
          haxelib install lime
          lime test ios
